# ♾️ Ouroboros Context

> Session: 2025-12-30
> Last Updated: 2025-12-30

---

## 🎯 Current Goal

Project initialized

---

## 🛠️ Tech Stack (summary from Phase 1)

- Language: TypeScript (see `tsconfig.json`, `typescript` devDependency)
- Runtime: Node.js (server-side scripts and tooling)
- Package manager / scripts: `npm` / `package.json` scripts
- Build: `tsc` for library builds; custom Node build scripts (`*.mjs`) for website
- Frontend: Static single-page site (pre-rendered HTML) built into `website/dist/`
- Webserver / TLS termination: Nginx (serves static files, TLS termination)
- SSL: Certbot / Let's Encrypt (certs under `/etc/letsencrypt`)
- Process supervisor (runtime): pm2-managed Node app (note: repo lacks pm2 manifests)
- Deployment pattern: ad-hoc `rsync`/`scp` uploads and server-side reloads
- Container/CI: No Docker/CI detected in repository

---

## 📁 Key Paths & Service Mapping

- SSH host: `15.157.26.254`
- Primary webroots observed: `/var/www/html` and `/var/www/serenixai`
- Nginx site configs: `/etc/nginx/sites-available/` (enabled via `sites-enabled`)
- Certbot / Let's Encrypt: `/etc/letsencrypt/live/<domain>/` and related files under `/etc/letsencrypt`
- PM2-managed Node app (environment note): listens on `127.0.0.1:5000` (proxied by nginx)
- Common deploy actions: `rsync` to `/tmp/dist_upload/` then sync into `/var/www/html/`; follow with `chown -R www-data:www-data /var/www/html` and `systemctl reload nginx`

---

## ⚠️ Basic Environment Notes

- SSH host: `15.157.26.254`
- Webroots: `/var/www/html`, `/var/www/serenixai`
- Certs: `/etc/letsencrypt` (Certbot-managed)
- Node app supervised by `pm2`, bound to `127.0.0.1:5000` (nginx terminates TLS and proxies to this)
- Nginx performs TLS termination and serves static site files

---

## ✅ Next Steps

- Verify Certbot auto-renew is active on the host (`certbot renew --dry-run` and `systemctl status certbot.timer`)
- Configure `pm2` startup on boot (`pm2 startup` + `pm2 save`) for the Node app
- Optional: purge Cloudflare cache after deploy (if Cloudflare is in front of the site)
- Confirm domain → webroot mappings on the server and standardize deploy targets

---

## 📝 Notes / Evidence

- Tech stack & service mapping assembled from repository files (`package.json`, `tsconfig.json`, `website/DEPLOYMENT.md`) and the Phase 1 project-arch notes.

