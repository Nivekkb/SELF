# Demo model key (optional). If unset, the demo uses the deterministic offline fallback.
GROQ_API_KEY=

# Optional demo model override.
SELF_DEMO_GROQ_MODEL=llama-3.1-8b-instant

# Optional event signing key override.
SELF_DEMO_EVENT_SIGNING_KEY=
